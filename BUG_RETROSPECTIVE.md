# Bouldering AI Pro 故障复盘（2026-06-15）

## 一句话总结

**本来 10 分钟能搞定的事，搞了一上午。原因是我一直在用「远程调试思路」去做一件「本地修文件、服务器跑命令」就能解决的事。**

---

## 错误复盘（按时间线）

### ❌ 错误 1：没有先确认 SSH 连不上就开了复杂方案

**问题：** PM2 报 `ERR_MODULE_NOT_FOUND`，我第一反应远程操作修代码，但实际 SSH 已经被 EDR 拦截。应该先 `ssh -v` 确认连通性。

**正确做法：** 先确认 SSH → 连不上就让用户加公钥，然后 SSH 进去操作。

### ❌ 错误 2：用了 paramiko 尝试密码登录

**问题：** 密码认证已被阿里 EDR 关闭，试一个本来就不可能成功的方案。

### ❌ 错误 3：远程编辑源码文件

**问题：** 超过 20 行的文件，远程 heredoc 很容易出错（转义、缩进）。应该本机改 → 本机 build → 仅上传产物。

### ❌ 错误 4：Nginx serve 静态 > 后端 serve 静态

**问题：** 花大量时间分析 `server.ts` 的路径逻辑，实际上 Nginx 直接 serve 静态文件、只把 `/api/` 反代到后端就绕过了所有问题。

### ❌ 错误 5：修 JS 产物而不是修源码

**问题：** 用 `sed` 直接改编译后的 JS，引入了新错误。

## 重来一遍的最佳路径

1. 发现 dist/ 丢失 → 让用户在服务器上重建或从备份恢复
2. Nginx 直接 serve 静态，不碰 server.ts
3. 本机改代码 + build，传产物到服务器

## 根因

`RouteGuide.tsx` 里 JSX 写了 `ref={canvasRef}` 但组件内没有 `const canvasRef = useRef()`（实际用的是 `imageRef`）。Vite build 时 TypeScript 不报这个错（视为全局变量），只在浏览器运行时抛出 ReferenceError。
